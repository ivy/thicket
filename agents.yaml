# Roster of thicket agents. Source of truth for identity, Slack apps, and
# tailnet provisioning. Task 002 defines the authoritative schema; this is a
# one-agent example so the scaffold has something to point at.
agents:
  - name: example
    description: Example agent used to exercise the toolchain.
    host: thicket-host
    user: thicket-example
