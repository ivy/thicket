{
  "display_information": {
    "name": "forge",
    "description": "Keeps the CI fleet green: watches builds, investigates red pipelines, bisects regressions, and proposes fixes as…",
    "long_description": "Keeps the CI fleet green: watches builds, investigates red pipelines, bisects regressions, and proposes fixes as reviewable patches rather than pushing anything itself.\n\nSkills:\n• CI triage: Investigates failing builds and finds the breaking change."
  },
  "features": {
    "app_home": {
      "messages_tab_enabled": true,
      "messages_tab_read_only_enabled": false
    },
    "bot_user": {
      "display_name": "forge",
      "always_online": true
    },
    "agent_view": {
      "agent_description": "Keeps the CI fleet green: watches builds, investigates red pipelines, bisects regressions, and proposes fixes as reviewable patches rather than pushing anything itself.",
      "suggested_prompts": [
        {
          "title": "CI triage",
          "message": "Why is main red?"
        },
        {
          "title": "CI triage",
          "message": "Which commit broke the deploy pipeline?"
        },
        {
          "title": "CI triage",
          "message": "Retry the flaky suite and tell me if it is actually flaky."
        },
        {
          "title": "CI triage",
          "message": "Summarize test failures from the last nightly run."
        }
      ],
      "actions": [
        {
          "name": "CI triage",
          "description": "Investigates failing builds and finds the breaking change."
        }
      ]
    }
  },
  "oauth_config": {
    "scopes": {
      "bot": [
        "assistant:write",
        "chat:write",
        "im:history",
        "app_mentions:read",
        "channels:history",
        "files:read",
        "reactions:write",
        "files:write",
        "groups:history",
        "mpim:history",
        "channels:read",
        "groups:read",
        "users:read",
        "canvases:read",
        "search:read.public"
      ]
    }
  },
  "settings": {
    "org_deploy_enabled": false,
    "socket_mode_enabled": true,
    "token_rotation_enabled": false,
    "event_subscriptions": {
      "bot_events": [
        "assistant_thread_started",
        "assistant_thread_context_changed",
        "message.im",
        "app_mention",
        "agent_session_stopped",
        "agent_session_title_changed"
      ]
    },
    "interactivity": {
      "is_enabled": true
    }
  }
}
