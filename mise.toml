[tools]
bun = "1.4.0"
go = "1.27.0"
pnpm = "10.34.5"

# Workflow linters. actionlint shells out to shellcheck for every `run:` block,
# so the two travel together.
actionlint = "1.7.12"
shellcheck = "0.11.0"
zizmor = "1.29.0"
pinact = "4.1.1"
